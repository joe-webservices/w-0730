
import React from 'react';

const AnnouncementBar: React.FC = () => {
  return (
    <div className="announcement-bar w-full py-2 text-center text-white text-sm animate-fade-in">
      Public beta is now live, join now and be one of the first to experience Adam.
    </div>
  );
};

export default AnnouncementBar;
